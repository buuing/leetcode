/**
 * @Author { String } ldq
 */
const minSubArrayLen = (s, arr) => {
  if (arr.length === 0) return 0
  let min = arr.length, sum = 0, i = 0, j = 0
  while (i === arr.length - 1) {
    
  }
  return min
}

console.log(JSON.stringify(
  minSubArrayLen(7, [2,3,1,2,4,3])
))
